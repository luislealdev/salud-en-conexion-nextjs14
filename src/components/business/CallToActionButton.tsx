'use client';
export const CallToActionButton = ({ phone }: { phone: string }) => {

    const handleClick = () => {
        
    }

    return (
        <p onClick={handleClick} className="f-size-18 p-20 bg-gray white-text radius-30">SOLICITAR AHORA</p>
    )
}
